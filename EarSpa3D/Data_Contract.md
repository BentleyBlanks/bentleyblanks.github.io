# EarSpa3D 当前契约

本文件约束 `index.html` 装配的拟物采耳入口；跨模块修改时读取涉及章节，并同步数据、实现和消费方。模块职责不代表永久人员分工，未登记的必要接口可随本次任务一起补充。

## 0. 当前玩法

真实器具接触与受力 → 因材质选择手法 → 松脱或真实碎裂 → 带出／吸入 → 落盘与清洁反馈 → 满意度结算 → 器具和皮肤经营。保留真实空间、材质差异及一指操作，不回退到固定向下拖、累计手势里程或缩小消除。

- 六种工具统一按住左键／触屏施力才平移，松手原地停留；旧 `toolDrag=false` 自动迁移，不再提供跟随鼠标模式。右键或触屏转向模式原地旋转，滚轮或触屏进退模式控制深度；键盘辅助也必须走实际工具接触。
- 选块、抓点及施力来自碰撞修正后的真实工作端；空点不吸附、不挪动工具，静止不加载。保留整件碰撞、长度、扫掠和工作面方向条件。
- 清洁度按实际收集质量增加；碎裂、刷拢、尚未落盘或吸入的物体不计收益。断口闭合，子块继承材料、运动与尚存附着，总质量守恒。
- 普通营业、油耳与练习分别使用各自的时限和结算规则；练习隔离营业进度，退出恢复进入前局面。小铺与设置暂停操作。
- 经营键保留 `earspa3d.shop.v1` 及现有旧档迁移；换客、跨日与小铺保留盘内物品，刷新重新开始收集。清盘不回退已结算收益，不改变新客初始质量。

## 1. 跨系统约定

- 1 世界单位 = 1 毫米，Y 向上、右手系；灯光按毫米尺度标定。空间查询返回缓存向量时立即复制，再进行下一次查询。器具显示几何与碰撞使用同一位姿。
- 耳道视角隐藏外部房间与角色，照明由相机头灯承担；取出视角由实时头部和耳廓承接。当前入口的探针与空间实现以 `Script_ImmersiveScene.js` 为准。
- 物理保留 240 Hz 子步、真实碰撞和独立质量约束；画质与性能优化不降低工具采样、扫掠精度或改变结算。具体薄壳／凝胶求解器边界见分项契约。
- 当前 UI 为暖黑半透明、米白文字、细衬线标题和米金选中态；模型和器具继续写实拟物。详情见分项契约“美术与声音”。
- 离线声音遵循根 Volcengine 规则。女声对白与叹气停用且不再生成；已试听的落盘三档及干／湿短刮保留原波形与音调。现场连续接触声按实际接触、位移与载荷门控，不能用旧接口中的“只能合成”覆盖已批准的采样通道。
- 普通页只提供只读 `__EarSpaProbe()`；仅 `?debug=1` 暴露可写 `__EarSpaDebug`。可选材质和音频缺失不得阻塞游戏；材质、几何与异步准备在取消或销毁时释放。
- 客人生成和物理输入使用传入的确定性种子，公共随机工具为 `Script_Util.MakeRng`；保持同种子可复现。当前渲染检查保留三角形 ≤180k、draw calls ≤120 的预算，不能通过删掉实际模型或降低碰撞精度达标。

## 按系统查阅

| 涉及内容 | 现行细节 |
| --- | --- |
| 输入、工具工作面、羽毛与刷拢 | [分项契约：工具与接触](Data_SystemContracts.md#工具与接触) |
| 薄壳、断裂与油耳 | [分项契约：物理与油耳](Data_SystemContracts.md#物理与油耳)、[油耳资产说明](Data_OilyWax.md) |
| 经营、练习、倒盘及换客 | [分项契约：经营与收集盘](Data_SystemContracts.md#经营与收集盘) |
| 物理设置、画质设置、预加载与性能 | [分项契约：设置与性能](Data_SystemContracts.md#设置与性能) |
| UI、解剖、材质与音频 | [分项契约：美术与声音](Data_SystemContracts.md#美术与声音) |
| 修改后的验证与浏览器证据 | [验收索引](Data_Verification.md) |
| 单文件导出与资产读取方式 | [打包说明](Data_Packaging.md) |
| 旧 `Script_Main/Hand/Wax/Ui` 接口或旧 §2、§5 引用 | [历史快照](Data_ContractHistory.md)，原编号保留；只用于兼容维护 |

新需求直接更新对应现行条目与检查，取代的方案留历史，不再在入口叠加日期补丁。
