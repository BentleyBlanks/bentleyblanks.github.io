# 骨架姿势 · 动画轨道 · 爬梯 · 接触戏 · 动画工作台

> TunnelLight1943 规范分册（从 CLAUDE.md 拆出，2026-08-18）。**改 `Script_Rig.mjs` / `Script_Climb.mjs` / 任何姿势或轨道之前读；改动画先 `anims`/`anim` 点名。**
> 硬规矩摘要与各分册的路由表在 [`../CLAUDE.md`](../CLAUDE.md)；本文件按需读，不自动进上下文。
> 往这里加条目：**【规矩】一句 / 【为什么】一句 / 【守着它的】测试名或 shot 判据**；
> 事故过程、日期、用户原话、实测数字写进 [`../Data_DesignHistory.md`](../Data_DesignHistory.md)，数值只写常量名。

## 爬梯：手脚钉在真横档上（2026-08-17 用户："你爬楼梯怎么和楼梯一点关联没有啊 就像是个平移一样"）

老版是三处各画各的梯子：`Art.DrawShaft` 随手撒横档，Core 按 0.34m 一档响一声，Rig 拿
一条正弦相位甩胳膊蹬腿，人整个按 smoothstep 从井口滑到井底——手脚从来没落在任何一道
横档上，当然像平移。**以后凡是爬的东西（梯子、楼梯、井壁上的脚窝）都走这一套**：

1. **一张落点表**：`Data_Ladder.LadderHolds(topY, botY, id)` → `{rungs, hands, feet}`
   （世界 y）。画笔、骨架、每档一响三处共用；换一架梯子/一段楼梯＝换一张表（楼梯只有
   `feet`，`hands` 给空表就不扒）。World 里 `ClimbSpecAt(x)` 按最近的竖井口取表。
2. **手脚落在哪档由几何定，不由相位定**：`Script_Climb.PlanClimb({holds, base, dir, bs,
   oneHand})`（纯函数，node 可测）。base 是演员脚线的世界 y（Core 的 `ground+lift`，
   一路平滑），它算出四肢各自扒在哪一号落点、正在换档还是扒稳、胯多高。步态照真人：
   大人**跨档步**（每只脚跳过另一只脚占着的那档，两脚永远在相邻两档；两手同理），
   `bs<0.75` 的小孩**并档步**（一次一档，后脚跟到前脚那一档）；四肢轮着动；胯从
   **最低那只脚**往上量腿长（`HIP_STAND` 故意给得比腿还长，只当上限）——于是撑着的那条
   腿几乎一直是直的、另一条膝盖抬到胯下一拳，只在够下一档那一下沉一寸。**两条腿都折着、
   膝盖朝外支就是"青蛙"**（2026-08-17/18 用户两轮退回：「像青蛙一样」「反关节的脚」），
   病根是**抬着的那只脚顶到了胯**：两脚踩相邻两档时上面那只离胯只有「腿长 − 一档」，档
   0.40m 配柱子 0.50m 的腿只剩 0.08，大腿翻过水平线、反解只能把膝盖翻到背后去。三道闸：
   横档 0.34m（`Data_Ladder`，留 0.15 余量）、`KNEE_ROOM`（抬着的脚至少在胯下 0.12·bs，
   宁可下面那条腿伸直了差一两厘米）、`wF`/`FOOT_LIFT_X`（空中占周期几成 / 换脚往后收多少）。
   SmokeTest 逐帧盯着"脚不许抬到胯上头"。井口那几档手扒着梯头、人要蹲下去（胯再压到
   胳膊够得着为止），不许站直了往下够。
3. **Rig 只做换算＋反解**（`ClimbPose`）：世界米→骨架局部（÷体型、减脚线），
   `SolveArm`/`LegIK` 两骨反解把手脚真的钉上去。**手举过头顶时肘的相对角必须折回
   (−π, π]**（`ArmIK` 里那段）——不折的话同一个肘弯算成 +240°，被当成反关节夹到 0，
   两条胳膊绷成两根直棍指着横档，实拍就是这样。
4. **爬梯目标不许再叠指数平滑**：目标本身按位置连续算出来，14/s 的平滑在 2m/s 的竖向
   速度下要落后 0.14m，手就从横档上滑下来。只在上梯子那 0.22 秒从站姿缓过去；World 的
   `liftNow` 对爬梯也**不缓**（老的 6/s 追赶让人到了井底还往下溜半秒）。
5. **抱着孩子爬**（`childArms` → `oneHand`）：近侧手照 childArms 静止帧兜着不动，只有
   远侧手扒、一次一档；胯抬到 `HIP_CARRY`（腿几乎直着）——她坐在他胯上、坐高按脚线算，
   他一蹲她就骑到脸上。
6. **梯子上是自己爬的，可停可掉头**（2026-08-17 用户："可以支持在半路停下的，就像勇敢的
   心那样"）：`Core.StepClimb` 三段——掀盖 → ride（按住 S/W 走，松手停在那一档，可掉头，
   到头还往那头推才出梯子）→ 盖回。`p.climb = {y, v, dir, phase, fromTop…}` 是真相，
   `p.climbT` 只是给老读者留的旗（在梯子上＝1）。速度按真人给：下 0.85 / 上 0.72 m/s
   （2026-08-18 用户：「哪有那么快下爬的」；3.6 米按住不松 4.2 / 5 秒），横档 0.34m
   一档、每档 0.4s；老的定时 1.5s 滑到底每档 0.14s，手脚钉得再准也是一团糊。停下来那一刻半空的手脚要**就近落到档上**
   （World 按 Core 的竖向速度给 `settle`，PlanClimb 把没走完的换档收回去/落下去），
   别按画出来的位移判——实拍冻帧位移也是 0，正爬着的一格会被拍成"停稳了"。
   跟着爬的妹妹待在进梯子那一头的那一侧、同停同走。**自动通关驱动器在梯子上要一直
   按着往目的层推**（`p.climb` 时 `input.climb = level==="under" ? 1 : -1`），只按一下
   人就挂在半路。
7. 守着这条的是 SmokeTest 的 `TestClimbPlanLocksToRungs`：三种体型×上下×抱不抱，
   落稳的手脚必在表上、逐帧连续、腿臂不拉断、整趟真的一档一档换过。
   **拍它**：`shot "c1_walk@x=29,hold=s,dur=1.2"`（下）／`"c1_walk@x=29,level=under,hold=w,dur=1.5"`
   （上）／停在半路：`shot "c1_walk@x=29" --pre "s*40"`（松手＝停）／抱着下：
   `shot c1_descend --pre "a*8,Ew*40,a*14,.*130,s*40"`；报告行里 `梯:ride@-1.61` 是到哪一档了。
   拿 `--eval "tl.world.__rigPose('player')"` 看关节角，胳膊直着伸出去先看 fore 是不是被
   夹成 0 了。两个坑：`--pre` 是**全局**选项，一条命令里每一拍都会先跑它；按着键的拍
   `dur` 走满之后现在会**冻帧**再截（不冻的话截图那一两秒里键还按着、人又爬了两米）。

### 关节只能往一个方向弯（2026-08-17 用户："人物还是有很多反关节的设置"）

`shinB/shinF` 与 `foreB/foreF` 是**相对上一节的局部角**，0 是伸直：

> **膝只能往后屈 ⇒ `shin ≥ 0`；肘只能往前屈 ⇒ `fore ≤ 0`。**
> 反号就是反关节（膝盖朝前折、胳膊在肘上朝后翻）。

病根是一条**写错的注释**——「肘要折死就给前臂写正值（把小臂折回来）」——被照抄了
**51 处**（静态姿势 + 轨道关键帧），于是全场的胳膊在肘上反着弯。已经全部翻号，
另外补了三道闸：

1. `ApplyPose` 里**硬夹**（膝 0~150°、肘 −155~0°）：谁再写反了，画面上顶多是伸直。
2. `AimFrontHand` 的 IK **先挑解剖上对的那组解**（fore ≤ 0）。两组解的手落在同一点，
   差别只是肘往哪边翻——所以挑对的那组**不会动手的位置**。老版只按"肘朝下"挑，
   一半的机位下挑出来的是反关节那组。
3. 翻号只保住"不反关节"，**落点还得重算**：`leanIn`（两只小手蜷在胸口）翻完是
   小臂世界角 −198°，手举到脸边上去了。要手落在胸前，是**上臂几乎垂着略往前
   （−24）＋小臂折上来（−104）**：肘在身前 0.09 / 低 0.23，手落在身前 0.29 / 低 0.09。
   改这类姿势先算落点，别照感觉写角度。

### 动画的流畅度：Hermite 穿过关键帧，别每段各自 smoothstep（2026-08-17）

- `SampleTrack` 老版是**每一段各自 smoothstep**。缓入缓出听着好，可它让速度在
  **每一个关键帧上都归零**——一段五帧的动作走成"停—走—停—走—停"，这就是"不流畅"
  的正主。现在走三次 Hermite（前后各取一帧算切线，张力 0.40），速度连着过关键帧。
  并且走的是**单调**那一支（Fritsch–Carlson：
  切线取两段割线的均值再夹住、折返点上归零），所以**一点都不会冲过头**——`hipY`
  因此不可能被插值推得踩进地里。（我先写的是定张力的 Catmull-Rom，合并时取了
  并行分支那版更好的单调实现。）
- 姿态混合改成**真正的指数平滑** `1 − e^(−14·dt)`。老版 `dt*12` 是线性近似：掉帧
  那几帧一步跨过去（>1 被夹住＝硬切），高帧率下又比设计的慢。
- **肢体是圆柱，不是剪纸**：`DrawLimb` 的暗部原来是 InkFill 的 `shade`（压在右下角
  的一块方），现在是**沿长边走的横向渐变**（背光侧暗、受光侧一线亮、两边再收），
  外加关节处**一道**衣褶。多给几道就成花纹了。

## 接触戏（打、夺、拦、推）——两条硬规矩

一维横版里，"谁碰到了谁"完全由**间距**和**落点角度**决定。这两条是 2026-08-10
第十一场重做换来的（用户原话：「这他妈扇耳光，夺走布什么的一个动画都没做」——
其实动作在，只是抡在三米开外的空气里）：

1. **动手之前先站到一臂之内（≤ 1.15m）。** 半个身位加一条胳膊就是这个数。
   隔着两三米抡得再狠，画面上是两个人各做各的操。每一拍先把两个人挪到位，
   再起轨道。回归断言 `TestContactStagingIsCloseEnough` 逐帧盯着这条
   （施 = slap/kickGut/buttStrike/snatchGrab/shovePush，受 = slappedFall/
   gutFold/struckFall/snatchLose/shovedBack/…，一旦有人抡起来而一臂之内
   没有人在挨，当场红）。
2. **成对的轨道：受方从负数 t 起。** 负的那一段是"等"，让挨的那一下正好落在
   施的那一下上（`buttStrike` 砸在 0.95s，所以受方写 `t: -0.95`）。
   受方轨道的**第一帧必须是挨打前的姿势**——`struckFall` 曾经第一帧就是跪姿，
   于是"等"的那一段里人先自己跪下去，枪托才慢悠悠抡下来。
3. **落点角度按实物量，不按感觉写。**（见下一节的世界角规矩）耳光第一版照
   抡枪托的幅度写成 −82°，手从孩子头顶上方半尺扫过去。正确做法是先量：
   兵的肩 1.22m、孩子的脸 0.91m、身前 0.4m ⇒ 世界角 −52°。
4. **手里的家伙要跟动作对得上。** 扇耳光时兵手里不能留步枪——枪是顺前臂挂的
   （ALONG_ARM），抡手那一下整支枪横扫过去，成了拿枪捅人。用 `carry: ""`
   压掉兵默认那支枪（`a.carry ?? 默认` 对空串不生效，专门用这一点），完事再还回去。
5. **一维线上两个人挤在同一处，只能靠深度分开**：倒地的人打 `a.rank = 1`
   （`RankDz(1)` 退一档），下一拍站过来的人就不会踩在他身上。
   **但退一档是 −1.6，退档的人绘制序号会掉到 walk 带底下**——他会被工作台、
   台上的料这些道具盖住，用完必须收回来（`a.rank = 0`）。

## 动画按名字点名：动画工作台（2026-08-17 用户定：「按照动画名称指定重做，而不是靠描述/截图画面」）

设置面板 → **调试 · 动画工作台**（`F4`，或 `index.html?anim=scoopChild` 直接选中一条）。
全屏、自己一台渲染器、**不碰世界**——但骨架是 `CreateRig` 出来的同一具，喂的是
`PoseRig` 同一套状态（World.UpdateOne 喂什么它就喂什么，相位走法照抄），所以看到的
就是游戏里那条动画，不是复刻。三栏：

- **左**：全部动作按名字列出——轨道（`TRACKS`）/ 姿势（`s.pose === "…"` 分支）/
  步态（走跑蹲猫腰爬爬梯挖提扛抱那几支状态分支，这里给了名字：`walk`/`run`/
  `stoopMove`/`climbChild`…）。搜索认名字、源码说明、节拍 id、谁在用。
- **中**：循环播、拖时间轴（关键帧刻度）、逐帧（←→）、换人（柱子 0.75/0.93、妹妹
  0.60/0.68、娘、爹、日军…按用法自动换）、翻朝向、关键帧叠影、手脚头轨迹、
  手脚离地量具（同 `PlayerLimbTips`）、「逐帧精确」关掉游戏里那层姿态混合。
- **右**：这条动画的一切——`Script_Rig.mjs:行`、时长/循环/帧数/关节、源码里的说明、
  关键帧表（每帧关节值 + 行内注释，点一行跳过去）、由什么驱动、用在哪几拍（文件:行 +
  节拍 + 谁）、此刻的关节值与离地。**「复制引用」**压成一行贴进对话：
  `轨道 scoopChild · Script_Rig.mjs:503 · 1.45s 单次 · 6 帧 · 用于 c1_descend / c1_tally · 柱子`。

命令行同一份索引：`anims [片段]` / `anim <名字>`（`Script_AnimIndex.mjs` 纯正则扫源码，
浏览器与 node 共用；轨道的 dur/loop/keys 在浏览器里以运行时 `TRACKS` 为准）。
**索引不是运行时真相**：改了 Rig 就重开工作台/重跑 CLI。新加轨道/姿势不用登记——
它扫的是 `TRACKS = {…}` 与 PoseRig 那串 `if`；但**步态分支的条件文字改了**要同步
`Script_AnimIndex.LOCOMOTION[].cond`（对不上会在清单里报「源码里没找到条件」）。
退回动画时**先开工作台点名**，再谈画面。

**人物美术样式浏览器的右栏就是它的小舞台**（2026-08-17 用户：「整身的样子有点小，
而且不能预览动作」）：`CreateRigStage` 那具真骨架按这个人戏里的体型（`DefaultPreset`）
跑他自己用到的动作（步态全给，轨道/姿势按剧本里谁在用挑，`EntriesForKind`），
「工作台 ▸」带着这条过去逐帧看。索引一次 fetch 两处共用（`EnsureAnimIndex`）。
另：`DrawCharacter` 收的是 `spec.scale`，传 `S` 没人读——整身平贴图一直 62px 小人就是这么来的。

## 静态姿势 ≠ 动画（2026-08-13 用户退回「主角互动、娘的过场、妹妹的动画都很生硬」）

判据一句话：**台词里带"过程"的动词，一律走关键帧轨道，不许只摆一个造型。**
那一轮实拍逐帧核出来的样子——娘"一把将她拉进怀里、上下摸了一遍"借的是**拽水桶**
那个 `haulIn` 并钉死在 poseK 0.55，相隔 4 秒的两张截图像素相同；掀翻板的四件事
（够环／滑手／蹭汗／掀开）挂同一个造型 5.4 秒；c1_count 那 24 秒妹妹一帧没动。

- **`FlashTrack(state, name, dur, who?)`**（Core）＝一次性关键帧动作，`until` 到点
  自动收回常态。`FlashPose` 从此只留给真的只有一格的东西。
- **关键帧之间怎么插，是"流不流畅"的正主**（2026-08-17 用户：「抱起妹妹的动画
  太蠢了 优化一下，不流畅」）。老版 `SampleTrack` 对**每一段各自 smoothstep**——
  缓入缓出是对每两帧之间说的，于是**每个关键帧上速度都归零**：一条四帧的轨道
  读出来是三下互不相干的推拉（蹲、停；抬、停；站、停）。而"写密一点让它顺一点"
  是所有人的第一反应，等于往病根上加料。现在走**分段三次 Hermite**，切线取前后
  割线（Catmull-Rom）并按 Fritsch–Carlson 夹住：速度只在**真正的折返点**（蹲到
  最低、抡到最高）归零，同向的几帧连成一条连贯运动；循环轨绕回去取邻居，
  **跨过末帧那一下也不再顿**。**不过冲是硬要求**——这套角度全是按几何量出来的
  （手要落在她胯上、膝要着地），普通 Catmull-Rom 在快慢段交界处会冲过关键帧
  十几度，那就是手插进人体里。
- **一个"有过程的动作"要三样东西齐了才不蠢**（同一轮换来的，抱妹妹是样板）：
  ① **起手不许从站姿重来**——前面 0.9 秒玩家一直按着，`scoopReach` 已经蹲下去
  把手伸到她腋下了，轨道第一帧要**接着那个蹲**往下走（老版轨道首帧是站姿，
  于是当帧弹回去、再蹲第二次）；
  ② **有预备动作**——抬人之前先往下再沉一寸。没有这一沉，"使劲"就只是位置在变；
  ③ **成对**——抱的人在动、被抱的人一动不动＝抱了个假人。她那一边要有自己的
  轨道（`armsUpWait` 举着手等 → `scoopedUp` 被扣住腋下踮起来 → 离地时**腿还垂着**
  → 坐上臂弯才收腿盘住）。**腿要晚于人离地**：一起收就成了"自己跳上去的"。
- **跟着轨道走的不只是骨架，还有位置与坐高**。老版一进 `carrying` 就把她的 x 挪进
  怀里、`lift` 直接给满，实拍量到他才蹲到一半（`scoopChild` t=0.48）她已经坐在
  0.42m 的半空中了。现在 tick 按 `player.track.t / SCOOP_DUR` 换算行程，横着滑进
  怀里与竖着升起来**共用同一条曲线**。
- **"抱"与"举"差在坐高上**（2026-08-17 量出来的）：上一版 `SEAT_LIFT=0.42` 把她坐在
  他**胸口**——她头顶 1.258m、他头顶 1.159m，**被抱的孩子比抱她的人还高一头**。
  抱孩子是**坐在胯上**（他胯 0.57 / 肩 0.93），头顶落在对方下巴那一线。现在
  `SEAT_LIFT=0.28`，实测她头顶 1.118 < 他 1.158、膝和脚都绕到他身后（盘住），
  他的近侧手落在 0.633 正在她胯（0.679）底下。
- **够不着就没有胳膊的事**：`scoopReach` 第一版让她贴到身前 0.34m，而他半蹲时肩
  落在身前 0.22m ——肩到她腋下只剩 9 厘米，比一条上臂还短，胳膊怎么摆都是一团
  折起来的东西（实拍读出来是"两条平端的直棍"）。**动手之前把人摆到 0.50m**，
  再按落点反算角度。同「先量够不够得着，再改动画」那条老规矩。
- **进度驱动的姿势在骨架里读 `s.poseK`，不是 `s.poseU`**：World 的 `PoseProgress`
  按姿势名从 vaultK/poseU/poseK 里挑一个、统一塞进 `poseK` 交给 Rig。直接读
  `s.poseU` 拿到的是 undefined，姿势冻在第一帧——跟"漏登记 PoseProgress"
  一模一样的症状，排查时两处一起看。
- hold 类节拍可声明 **`holdTrack`**：按住跑循环轨道、松手当帧撤掉。
  **但 `holdPose` 必须留着**——失败判定读的就是 `player.pose === "shelter"`
  （c1_bell / c1_hide），换掉当场判失败。PoseRig 里 track 优先于 pose，
  两者共存＝画面走轨道、判定读姿势。
- **进度驱动的姿势必须登进 `PoseProgress`**（Script_World.js）——漏登记就冻在
  第一帧。长按类（`steady`）与绕圈类（`stroke:"circle"`）尤其容易漏。
- **抱着人走是"走姿"不是姿势**：`state.player.childArms`（每帧清、抱着的那一帧
  自己立）。pose 会把走路整个顶掉，这类"只占用上半身"的状态一律另开一档。
  被抱的那个也要有对应姿势（`heldChild`）——`leanIn` 是**站姿**，挂在被抱的人
  身上就是两条腿笔直垂着。
- **抱人的几何：肘要吊下来。** `armF -84` 是把**肘**平端到身前，肘先占掉 0.25m、
  手再往外 0.24m ⇒ 手落在身前 0.58m，而孩子的身子中心只在 0.31m——胳膊从她身上
  直穿过去指着背后的空气。正确形是**上臂几乎垂着（−10~−40）、前臂横过来兜住**。
  **这条 2026-08-15 又被破了一次**（用户：「柱子抱妹妹完全不对」）：`childArms`
  当时写成 `armF −96 / foreF 38`，实测手落在身前 0.61m、离地 0.52m，而她的身子
  在身前 0.26m ——他在她身外 0.35m 的空气里认真兜着。现在是 −35/−73（近侧托屁股）
  与 −48/−103（远侧搂后腰），量出来手落在身前 0.23m / 高 0.82m，正压在她胯上。
  连带三条：
  · **一件事只许有一处真相**：`scoopChild` 轨道的末帧注释写着"＝childArms 的静止帧"，
    改了静止帧就得改末帧，否则轨道一到期人当帧弹一下；
  · **抱着的那个要贴住**（身前 0.20m，不是 0.26m）且**脸朝着他**——同向的话她那两条
    搂脖子的胳膊就搂在空气里；
  · 她画在他之后（`heldByPlayer` → `DRAW_NUDGE_HELD`），否则贴在胸前的人被胸口盖住。
- **抱着的人跟着换层，别中途"放下"再摆到目的地**（2026-08-15 用户：「也没有抱着
  下地道的设计，妹妹直接瞬移到了地道下面」）。老版在走到窖口那一步就
  `carrying=false` 并顺手 `sis.level="under"; sis.x=30.7`——一句话把人挪下一层又
  挪开 1.4 米，玩家看到的就是瞬移。钉位要**连 level 与 lift 一起钉**
  （`sis.lift = player.lift + SEAT_LIFT`），他一级一级下梯子她就在怀里跟着下；
  真要放下时落点用**他这会儿站的地方**，不是写死的坐标。
  配套：爬梯那支骨架要有"抱着爬"的变奏（`PlanClimb` 的 `oneHand`：远侧手扒横档、
  近侧手保持兜的角度不动、胯抬高腿直着），不然抱着下窖会退回"两只手都在爬"，
  怀里的人成挂件。
- **两个人要看得见是两个人**：0.31/0.44m 时两颗脑袋叠在一处、孩子被大襟长摆
  整个吞掉；0.59m 才读得出「搂着」，而且要算上受方自己往施方倾的那 0.2m。
- **静态姿势也要有生命体征**：Rig 末尾按白名单叠呼吸。两条不许碰——① 靠
  `AimFrontHand` 反解落在挂点上的那几支叠角度就把手推离把手（RenderHealthTest
  会红）；② 跪/按/咬布不许动 hipY（膝与手贴地，一起伏就陷进去）。
- **`lift` 走缓动**（World `UpdateOne` 的 `s.liftNow`）：剧本写 `lift = 0.52` 是
  当帧生效的，孩子凭空升高半米。>1.0m 的大跳仍瞬时（换层/跳幕）。
- **跑不是把走路放快**：`gait` 0..1 由实测速度给（1.5→3.2m/s），步幅/小腿/
  躯干前倾/手臂摆幅一起变；**步频再按体型折算**（`moved * 3.4 / bodyScale`），
  否则矮一头的妹妹踩着风火轮跟着走。
- **脚陷地的通治式**：`hipY = 0.31·cos(大腿) + 0.31·cos(大腿+小腿) − 0.62`。
  改完用 `shot --probe` 的 `PlayerLimbTips` 对一遍（站姿脚 ＝ 鞋帮×体型，柱子 0.072）。
- **`ClearPoses` 要连 `poseT/poseU/poseK/poseStrain` 一起清**（演员那一路也是）：
  只清 pose 的话，上一拍没到期的 FlashPose 会在下一拍中途把新摆的姿势抹掉。
- **改动画之前先量"她在不在画框里、够不够得着"**：那一轮有三处是摆位/机位错
  ——娘站在洞口正中背对着翻板去掀（板铰链在西、活动边在东，人得站到活动边
  外侧）、窖口五个仰角机位把她整个框在画外、手腕特写八个机位偏着 0.63m。
  **道具的时长也要和轨道对齐**（`state.lid` 因此加了 `delay`）。

## 骨架姿势（改 Script_Rig 前必读）

- **上臂角 + 前臂角相加就是这条胳膊的世界角，0° 指向下，越负越往前上方转。**
  超过 −90° 就已经过了水平、开始"举起来"了。`shelter`（搂住孩子）曾经是
  −154°、`leanIn`（手蜷在胸口）曾经是 −118°——读出来全是"举着手"，手压根没
  落到人身上。写姿势前先算落点：手要停在哪儿（离地多少米、身前多远），
  反推出世界角，再把它拆成上臂与前臂。**肘要折死就给前臂写正值**（把小臂
  折回来），别靠继续加负数。

- **`poseK` 的来源判据是"在不在翻越"，不是 vaultK 有没有值。** `p.vaultK` 平时
  就是 0（不是 undefined），所以 `p.vaultK ?? p.poseU` 里的 `??` 永远命中那个 0，
  进度驱动的姿势（刨料 planePush、顶撑木 braceUp）一辈子拿不到进度、冻在第一帧。
  正确写法是 `p.vaultT > 0 ? p.vaultK : p.poseU`（演员那边看 `a.pose === "vault"`）。
- **两条胳膊挂在 root（胯）上，不是躯干上**，肩点由 `ApplyPose` 按躯干角算出来
  （躯干折多少肩就跟多少）。站着走着差几度看不出来，**躯干一折下去就是成败线**：
  这行以前只写了个固定的 y，爬行那一拍人趴下去了胳膊还悬在胯上方半米——
  用户的原话是「手都飞了」。改这段要连所有大角度姿势一起看。
- **四肢角度 0° = 指向下；但脚那块贴图 0° = 指向前**。所以要让鞋底贴地往后铺，
  要的是「大腿+小腿+脚 ≈ 180°」而不是「≈0°」。忘了这条，脚就以踝为轴穿到地板底下。
- **常态姿势里"没在做事的那两只手"最容易翻车**（2026-08-10 用户截图退回地道里的
  三个乡亲：「这三个傻逼在干什么？在作死啊」）。猫腰（stoop）原来给的是
  `armF -44 / foreF -36`——**胳膊角是世界角**（挂在 root 上），加起来向前 80°，
  于是两条胳膊一起平端在胸前直指前方，两条腿又给了一模一样的角度叠成一条：
  一排人站成僵尸。三条判据，闲着的姿势逐条过：
  ① **手要顺着重力吊下来**（0° 就是吊着），闲着的人不会平端双手；
  ② **肘要折一下**——直吊的胳膊和小腿几乎平行，侧视里一个人成了四根竖棍，
     读出来像虫子；肘一折有了拐角，胳膊和腿才分得开；
  ③ **左右不许同角度**，两条腿也要一前一后错开，否则侧视里只剩一条。
  再加一条构图的：**低头的姿势读作"干呕"不是"猫腰走"**。腰折下去多少，
  脖子就要抬回来多少——判据是**头的世界角**（torso + head）要在 −20°~0°，
  脸朝前上方。老版腰 46 配脖子 −34，世界角 +12，脸冲着自己的脚。
- **过场里三个人站一排朝同一边＝复制人**。姿势修好了也救不回来：
  朝向要互相对着（说话的朝里、听的转过来），间距错开，说话的那个得有动作
  （地道里指洞顶用 `pointLow`——它**自带猫腰**，站姿版另写，别拿它凑合）。
  演员的步频/呼吸相位也按 id 打散（World 建演员状态时播种），
  都从 0 起步的话一队人会踩着一模一样的步子。
- **躺着的姿势不许掰关节，整具骨架转 90°**（2026-08-12 用户退回妹妹的睡姿：
  「妹妹这样的动作睡觉的？」）。老版是把站姿的关节一根根掰到接近水平
  （躯干 88°、大腿 −88°…），实拍出来是一堆散落的方块：袄子下摆横着支出去、
  脑袋斜插在一边、小腿戳出画。**病根不在角度**——侧视骨架的每一块都是照
  "竖着的人"画的，关节掰平只会让贴图各躺各的。现在 World 的 `LIE_POSES`
  见到这类姿势就把整具 group 转 ±90°（再挪半个身长、垫半个身厚），
  Rig 那一支只写"站着的时候蜷成什么样"。
  配套两条：① 姿势角度要**在躺下之后那个画面里反算**（转过去之后"朝前"就成了
  "朝上"：照站姿手感写胳膊，人躺下会直挺挺地举着手）；② 倒的方向是**往背后
  倒**（头落在朝向的反侧、肚子朝上），所以剧本里想让谁的头冲哪边，改 heading。
- **接触戏的落点是量出来的，不是摆出来的**（2026-08-14 用户退回「这个陌生人
  你完全没做出来」）。§9「一只手从黑暗里伸出来抓住手腕」老版是：地上贴一张
  静帧手（strawArm 道具，已退役）＋柱子站在**一米开外**自己举着胳膊——
  两只手中间空着半个身位，说话的人从头到尾不在画面里。现在两条轨道对在同一
  个点上（伤员 `strawReach`／`strawSink`，柱子 `wristSeized`），站位用
  `world.LimbTipsOf(<演员id>)` 量：伤员躺在 27.1、肩在 27.56、探出来的手落在
  **28.10 / 离地 0.53**；柱子蹲在 28.6 时前手落在 28.09 / 0.56。差一厘米以内
  才叫"攥住"。**躺着的人够不到站着的人的手腕**（肩离地才 0.3 米），所以这一镜
  柱子必须先蹲下——蹲下那一格不是修饰，是这场戏成立的前提。
- **躺着的人伸手，角度要在躺下之后反算**：`sleep` 转 90° 之后，站姿的「上」
  是躺下的**东**（头那一侧）、站姿的「前」是躺下的**上**。要手落在东 +0.40／
  高 +0.25，站姿里得把胳膊举过头顶再前偏 32°（armF ≈ −148°、前臂几乎伸直）。
  另外 `rotation.z = -角度`：`armF −100` 才是"手伸到身前 0.5 米"。
- **触地是可以量的，就该量**：`world.PlayerLimbTips()` 返回两手/两膝/两脚离地
  多少米（正=悬空，负=陷进地里）。新姿势按骨长算完角度后，用它对一遍——
  爬行现在是 手 −0.01~+0.09 / 膝 0~+0.04 / 脚 ±0.02，头顶 0.73 与
  `POSTURE_HEAD.crawl`(0.72) 对得上。姿势"像不像"靠眼睛，"站没站在地上"靠这个。

## 扛与提、携带物标签表（2026-08-18 从项目记忆并入）

- 【规矩】`carry`（扛在肩）与 `hold`（提在手）是两档姿势：hold 按 `holdW` 0..1 插值——胳膊坠直、身子后仰配重、空手那侧甩得更开、步子小一档；挂点跟着变（胳膊坠直后桶别杵在地上、别盖住人的剪影）。
- 【规矩】携带物的标签统一走 `BUCKETS` / `ALONG_ARM` / `HAND_HELD` / `HOLD_WEIGHT` 四张表（`Script_World` 顶部），画布、挂点、姿势都从它们取；`UpdateOne` 收的是**标签**不是布尔。同一只桶别再叫四个名字。
- 【规矩】注解写了、代码没干的老账要先 grep 再信注解（`HAND_HELD` 注解列了破袄子而数组里没有→衣裳贴在人脸上；妹妹的 `lift` 从某拍起一路挂着→浮在半空）。
