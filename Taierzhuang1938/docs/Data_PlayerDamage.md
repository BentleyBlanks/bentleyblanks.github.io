# 玩家挨打这条链（口径、TTK、受击反馈）

> 起因是一句实话：「AI 会把我打死得太快了，而且基本上直接就死，连红框的晕角提醒好像都没有。」
> 这份文档把那句话拆成三件独立的事，各自记清楚改了什么、为什么、怎么复测。
> 回归口：`node Taierzhuang1938/Script_DamageTest.mjs`（退出码即成败）。

## 2026-09-11：无敌一致性与失血范围反馈

第一关关键队友阵亡会触发任务失败，但失败面板此前无条件使用“你已阵亡”标题；
开启无敌且玩家仍然存活时也会出现这个误报。现在队友阵亡显示“任务失败”及队友名字，
玩家自身阵亡才显示“你已阵亡”。无敌保护玩家，任务对关键队友的存活要求仍然生效。

白刃测试场的场景重开曾直接清掉玩家无敌标志，菜单保存值仍为开启；现在重开也从
`ApplyDebugOptions` 恢复同一份配置。`PlayerController.Kill` 补上无敌保护，避免直接死亡
入口绕过 `TakeHit`；开启时立即清理旧红闪、受伤方位及待播放受伤事件。
这些是已定位的代码漏洞，不推定每一次玩家阵亡都来自白刃测试场。

来弹断开细弧保持红色，以形状和强度区别擦身弹与实际受伤；中央命中记号也改为红色。
血晕从失血开始持续加深，并按血量收缩中央透明椭圆、让血污纹理向内扩展。
濒危搏动随生命降低加快；常驻的“生命危急”提示由文本表提供，恢复或死亡时清除。
全部阈值见 `Data_Tuning_Hud.VIGNETTE`，不更改敌人伤害、流血和绷带治疗数值。

`Script_IncomingFireBrowserTest` 验证六档生命的范围/透明度单调变化、红色警告、恢复、
减少动画设置、真实调试开关、阶段重建、各种致命伤害以及关闭无敌后的死亡/检查点恢复；
`Script_MeleeQteTest` 增加重开实验场仍然无敌的回归。
`Script_DeathMenuTest` 通过真实队友死亡触发失败，验证无敌玩家仍存活且标题不再误报。
下节为前一版固定覆盖范围与沙金色来弹提示的历史记录。

本次验证：69 项 quick、33 项 HUD/infra prepush，以及白刃场、伤害、死亡菜单三项浏览器
回归全部通过。已检查 1280×720 与 745×377 六档生命/红色来弹截图。
实测罗班长死亡后玩家仍存活、生命为 100，面板显示“任务失败 / 罗班长 · 阵亡”。
这些是组件与 UI 验收，不作为重新跑完整关卡或性能提升的证据。

## 2026-09-11：方向与低血量 UI 素材更新（上一版）

第一级内置 Imagegen 制作两张带真实 alpha 的 PNG，见 [素材与完整提示词](../Texture/Hud/Data_HudDamageArtwork.md)。
受击弧改为破碎血色纹理；擦身弹复用纹理并淡化成沙金色，加断开的细弧区分，没有方向尖角。
五个方位仍共用一张纹理、按世界来弹向量随当前 yaw 旋转；伤害、近失弹判定、合并和寿命不变。

低血量用不规则周边血污替换均匀红渐变，中央保持透明。血量与即时红闪仍取 max，
濒死在 `::before` 子层做双拍 opacity 动画，不覆盖父层血量透明度，也不逐帧过滤全屏纹理。
恢复、重生后按原血量状态消退；系统启用 reduced-motion 时保留警示但停止搏动。
原有 `VIGNETTE` 数值、受伤音效与心跳音效不变。下文旧 CSS 数字与实测账属于历史记录。

回归入口为 `Script_IncomingFireBrowserTest`，增加实际素材解码和中心 alpha 验证、
100/60/22 HP 分级截图、濒死动画、恢复与 reduced-motion 检查，并保留四方位与生命周期断言。

本次已通过 45 项 quick、32 项 HUD prepush；检查过 1280×720 与 745×377 的真实关卡 HUD 截图。
HUD 父层在 100/60/22 HP 时透明度分别为 0 / 0.134 / 0.498；转身后右侧来弹转为正前，
五个方位到期与重生均清空。两张素材实际解码成功，中心采样 alpha 最大为 1/255。
DamageTest 伤害回归通过（27.1 s），Pages 合并入口本地构建通过。这些是 UI 和组件验收，不作为整关通关或帧率证据。

## 2026-09-11：短距离威胁与来弹方位

修正前的真实 TryFire 定种子靶场：第一关剧本倍率下，12 m 站姿 340/600（56.67%），
18 m 140/600（23.33%）；远处射手占满名额时，12 m 射手不能新选玩家，也不能接替远处令牌。
上一轮 2–5 m 验收不能覆盖壕沟对面的短距离交火。

`CLOSE_RANGE` 的完整近距区延至 15 m，仍在 25 m 平滑恢复原远距平衡；
25 m 内允许近处可见敌人参与目标选择，并按既有距离差条件接替更远的攻击令牌。
同时造成伤害的人数上限、真实暴露采样、友军射击走廊、枪口朝向、压制与单发伤害上限继续生效。
不是看到敌人开火就保证伤害：掩体挡住身体时仍然可以安全躲避。

HUD 沿用 COD 战役的准心外围受击红弧，同时加一层本作的近失弹预警：

| 情况 | 反馈 |
| --- | --- |
| 敌弹实际从头/胸旁经过 | 浅金色断开空心弧与方向尖角，1.3 s 消退，不产生血色红闪 |
| 真正受伤 | 红色实心弧与方向尖角，2.2 s 消退，保留受伤红闪与既有受击音效 |
| 连射与多方向 | 同方向合并，最多五个方位；后续擦身弹不会降级或无限续亮受伤标记 |
| 转身 / 重生 | 世界方位按当前朝向转换；重生清空，不追踪敌人后续移动 |
| 墙挡弹道 / 远离玩家 / 子弹终点尚未到玩家 | 不产生近失方向提示 |

方向预警在射击规则侧测量真实弹道有限线段到玩家头/胸采样的距离，并复用遮挡查询；
不依赖 AudioContext 或音量，静音也能看到。旧随机近失值只保留一次抽样以稳定旧伤害骰序，
压制量改按真实掠过距离计算。朝友军射击的日军流弹经过玩家也可触发预警。

回归：`Script_AiCloseRangeTest` 增加 12/18 m 命中、12 m 满名额接替、实弹近失与墙后无提示；
`Script_AiShootingTest` 检查有限弹道、背离及遮挡；
`Script_IncomingFireBrowserTest` 检查真实 HUD 接线、四方位、转身、两类形状、连射合并、
红弧优先、消退/重生清空和小视口截图。靶场与 HUD 夹具均不冒充正常通关证据。

本次同种子、同剧本倍率、每档 600 发的实测（站姿）：

| 距离 | 修正前 | 修正后 |
| --- | --- | --- |
| 2 m | 522/600 | 522/600 |
| 5 m | 509/600 | 509/600 |
| 12 m | 340/600 | 469/600 |
| 18 m | 140/600 | 365/600 |
| 25 m | 25/600 | 25/600 |
| 60 m | 19/600 | 19/600 |

12 m 满名额场景由不能选择玩家、不能取得令牌，变为两项均成功，令牌总数仍等于原上限。
2 m 正常射击节拍首发造成伤害在 0.85 s；全遮挡伤害与方向标记均为零，
预警弹只有 near 标记且 hitFlash=0。DamageTest 与 AiBehaviorTest 本次复测通过；
HUD 四向为 0° / 90° / 180° / −90°，右转后原右侧来弹转为正前。

交付前检查：53 项 quick 通过；IncomingFireBrowserTest、AiCloseRangeTest、
DamageTest、HudPromptBrowserTest、AiBehaviorTest、BootTest、
FirstLevelMissionBrowserTest（正常输入 `--campaign --audio`）全部通过。
本次完整关卡检查运行 751.7 s，结束状态 Complete、failed=false；这不是靶场 TTK。
已查看 1280×720 与 745×377 的本地 HUD 截图。过程截图与原始统计只留本地。

合并掩体射击门禁后，近距、伤害、HUD 与 AI 行为四项再测通过。整关首轮在厨房支援
检查失败：队友仍以约 3.5 m/s 正常追赶，但更快的清敌过程让玩家比队友先到。
通关驱动改为在村口先自然会合（最多 20 s 等至少两名队友进入玩家 15 m 内），再攻入厨房；
原有厨房至少两人在 25 m 内、所有存活队员在 45 m 内的断言保留，没有改位置、血量或任务事实。
复测厨房四人距离约 12.5–17.2 m，完整 `--campaign --audio` 在 711.5 s 后到达 Complete。

最终吸收 `7fa6a44b6` 的开局交火/护送修复后，重新通过 AiCloseRangeTest、
IncomingFireBrowserTest、FirstLevelOpeningContactTest；`--profile=prepush --domain=infra`
26 项全过，包含 Pages 合并包的实际启动。该补测针对新合入的开局与射击链，
整关记录仍是上一段的 711.5 s，不冒充又跑了一次整关。

## 2026-09-10：近距离射击修正

此前第一关前沿的剧本折扣与玩家折扣在贴脸时照常叠乘。真实 `TryFire` 定种子靶场中，
2 m 站姿 600 发仅中 25 发（4.17%），正常节拍十秒五发没有一次伤害。
本次将近距参数集中到 `Data_Tuning_AiShooting.CLOSE_RANGE`：近距逐步取消远程交火的命中与姿态折扣，
缩短必偏预警窗口，并按身体张角计算瞄准容差；达到 `fadeOutM` 后恢复原来的命中链。
`COMBAT.player` 的枪伤、部位倍率、单发上限、流血、出生保护均继续使用原表。

同一靶场修正后：2 m 站姿 522/600（87%），5 m 站姿 509/600（84.83%）；
2 m 正常节拍首发在 0.85 s 造成伤害。25 m、60 m 的配对结果与修正前一致。
墙体完全遮挡、朝向背离目标和剧本命中倍率为零时仍不造成伤害。
这些是隔离射击链的统计，不是整关死亡时间或正常通关记录。

新增回归：`node Taierzhuang1938/Script_AiCloseRangeTest.mjs`。
旧文档下述 TTK 账的适用范围为原 25 m 靶场，不能再解读为贴脸也应打十几秒。

本次交付验证：`--changed=origin/master --profile=quick` 53 项通过；
`--profile=prepush --domain=ai` 44 项通过；另跑 `DamageTest`、
`FirstLevelMissionBrowserTest --campaign --audio`、`BootTest`，三项全部通过。
正常输入通关耗时 693 s（运行测试的实际时间），结算为 Complete、failed=false，玩家剩余 46.71 HP。
已查看本地壕沟接敌、前沿交火与通关截图；截图及靶场原始数据仅留本地。

---

## 一、修之前是什么样（实测账，不是估算）

伤害口径原来散在**三个文件的四处魔法数**里，没有任何一处知道合起来是多少：

| 位置 | 数字 | 管什么 |
| --- | --- | --- |
| `Script_Ai.TryFire` | `× 0.55` | 枪伤 → 玩家 |
| `Script_Ai.TryBayonet` | `× 0.5` | 刺刀 → 玩家 |
| `Script_Combat.Blast` | `× 0.7` | 爆炸 → 玩家 |
| `Script_Player.TakeHit` | 头 `× 3.4` / 躯干 `× 1.0` / 四肢 `× 0.62` | 部位倍率 |

乘出来是这样：

- 三八式命中躯干 `72 × 0.55 = 39.6` → 三发一条命；
- **爆头 `72 × 0.55 × 3.4 = 134.6`，满血一枪毙**，而爆头掷骰占 8%
  —— 也就是说任何一次交火，第一发就可能是结局，玩家来不及知道自己在挨打；
- 三个射手（`COMBAT.maxShootersOnPlayer = 3`）在 25 m 上，
  靶场实测从满血打到死 **≈ 5—6 s**（`Script_DamageTest.mjs` 第 2 节会把旧口径重跑一遍做对照）；
- 伤口流血是叠加且几乎不自愈的（衰减 5%/s），四个躯干伤口 = 10.4 HP/s，
  两卷绷带追不上 —— 那不是「慢性死亡」，那是一块十秒的秒表。

而这 5—6 秒里屏幕上**几乎没有事发生**：

- 暗角不透明度写死成 `Clamp01(1 - health/70) × 0.62`。满血挨一发三八式之后
  剩 60.4，算出来是 `0.085`，再乘暗角边缘那层 `0.52` alpha ≈ **0.044**。
  数学上不是零，视觉上就是没有；而 70 到 0 只隔两发。
- 没有任何**事件级**反馈：暗角只是血量的函数，没有「刚刚挨了一下」这件事。
- 没有来弹方位：玩家不知道该往哪边躲。
- **没有声音**。`Audio/Sfx/AudioSfx_Hurt_01.mp3` 与 `AudioSfx_Heartbeat_01.mp3`
  早就烘好了、`Script_Audio` 里 `hurt` / `heartbeat` 两条配方也在，
  但全仓库一行没播过。
- 难度档里的 `aiAccuracy` / `playerDamage` / `suppressionScale` 三个字段
  立在 `DIFFICULTY_PRESETS` 里，**一处都没被读过** —— 换「体验/标准/写实」
  只改得动弹道重力、自由瞄准、铁瞄偏心和过热四件事，挨打的强度纹丝不动。

---

## 二、口径全部回到数据层：`COMBAT.player`

`Data_Battle.mjs` 里新增一个块，上面四处魔法数与部位倍率一律从它读。
**要调挨打的强度只改这里，别再往逻辑里写第五个魔法数。**

| 字段 | 值 | 说明 |
| --- | --- | --- |
| `accuracyScale` | 0.70 | 命中率单独给玩家一档（见下） |
| `bulletScale` | 0.33 | 枪伤 → 玩家。三八式 72 → 23.8 躯干，四发多一条命 |
| `meleeScale` | 0.42 | 刺刀 110 → 46.2：重，但不是读盘 |
| `blastScale` | 0.55 | 爆炸 → 玩家 |
| `aimScatterM` | 0.24 | AI 打玩家的部位**不抽概率**：照躯干中点瞄、在瞄点周围按这个 1σ（米）散点，射线去碰玩家自己的命中几何（`Script_PlayerHitbox`）。45 m 上站着约 5% 爆头、趴着约 15%（头露在最前面）；AI 打 AI 仍是 0.08 那一掷 |
| `headMultiplier` | 2.0 | 爆头倍率（对 AI 仍是 3.4） |
| `limbMultiplier` | 0.50 | 四肢 |
| `maxBulletDamage` | 62 | **单发对玩家的硬上限：满血挨一枪必活**（安全网，见下） |
| `bleedScale` | 0.50 | 伤口出血速率 → 玩家 |
| `maxBleedPerS` | 5.5 | 流血封顶：伤口叠加不许变成必死的秒表 |
| `firstShotGraceS` | 1.1 | 刚锁上玩家的这段时间内开的枪一律打偏 |

### 部位由几何判，不抽概率（2026-09-07）

`headChance` 已经退表。玩家现在有自己的命中几何（`Script_PlayerHitbox.mjs`：按站/蹲/卧摆的头球 +
躯干/四肢胶囊），`AiDirector.PlayerHitPart` 照躯干中点瞄、在瞄点周围按 `aimScatterM`（1σ）散一个点，
从枪口向它射线，碰到哪根胶囊就是哪个部位。两条不许破的账：

- **命中率那一掷不动**（`accuracyScale` / 姿态折让 / 首发必偏全在它前面）。几何只回答「打中哪儿」；
  散出去没碰到身体的那一支算躯干，不许让几何把命中率偷偷再打一次折。
- 站着以躯干为主（45 m 上约 5% 爆头）；趴着头露在最前面、躯干藏在头后面，爆头占比升到约 15%，
  但 `prone × 0.45` 的命中率折让仍让卧倒的每发期望伤害低于站着 —— `Script_PlayerHitboxTest.mjs`
  的散点统计与 `Script_DamageTest.mjs` 的「趴下比站着活得久」两条一起看着它。

顺带修的一条：AI 打玩家的瞄点以前一律 `position.y + 1.1`，趴着的玩家头顶 1.1 m 是空气，曳光全从头上飞过；
现在瞄的是 `PlayerAimPoint`（躯干中点）。

### 为什么命中率另开一档，而不去动 `aiAccuracyBase`

`aiAccuracyBase` 同时决定**日军和中国兵互相怎么打** —— 全场七十个人的消耗速度、
阵地什么时候被啃穿、票池掉多快，全挂在它上面。为了让玩家好受一点去调它，
等于把整场仗的强度一起改了。

玩家这一侧另有理由：他是一个人对一条战线，而且没有「被打中就趴下」的 AI 行为兜底
——同样的命中率落在他身上就是纯粹的秒表。所以 `accuracyScale` 只在
`s.target.isPlayer` 那一支上乘进去，AI 互相打的那一支一个数没动。

### 单发上限：只管小口径，而且是安全网

`maxBulletDamage` 只在 `info.bullet` 时生效。炮弹、集束、掷弹筒照样能一下要命 ——
那是它们应得的，而且它们有啸声、有落点标记、有 1.6—2.6 s 的预警：
玩家是「没躲开」，不是「没看见」。

对小口径来说它平时也不会被触发：现在最重的一发是九二式重机爆头
`92 × 0.33 × 2.0 = 60.7`，刚好在 62 下面。留着它是为了将来谁往 `Data_Weapons`
里加一支更重的枪时，「满血挨一枪必活」这条不会被悄悄推翻。

### 首发必偏（`firstShotGraceS`）

`Soldier.playerLockAt` 只在**从别处切到玩家**那一刻打戳（不是每次 `Think` 都续，
否则窗口无限延长 = 永远打不中）。`TryFire` 在窗口内把 `acc` 直接压成 0。

这一发**照样有枪口焰、有枪声、有近失弹压制**，它买的就是那一下：
子弹先从耳边过、暗角先亮一下，玩家才有得反应。老兵从冷瞄具到第一发本来也不准。

配合出生保护（`spawnGraceS = 3.2`，期间没人会锁玩家），重生之后实际有
**3.2 + 1.1 ≈ 4.3 s** 的找掩体时间。

---

## 三、受击反馈：把「我中弹了」变成一件看得见听得见的事

三条独立通道，都挂在 `PlayerController` 上，由装配层每帧读走：

| 量 | 谁读 | 表现 |
| --- | --- | --- |
| `player.hitFlash` | `Hud.SetHurt` | 一次中弹的红闪。强度按**这一发的实际伤害**定，与剩余血量无关；自己按 1.7/s 衰减 |
| `player.hitMarks[]` | `Hud.SetHurt` | 来弹方位（存**世界方向**，HUD 每帧按当前 yaw 转成屏幕角，所以转身之后指示器跟着转）。寿命 2.2 s，最多 5 条 |
| `player.hitEvents[]` | `Script_Main.PlayHurtCues` | 一次性事件队列：`hurt`（闷哼）与 `heartbeat`（濒死心跳）。取完即清 |

暗角现在是**三层叠在同一张图上**（前两层取 `max` 而不是相加 ——
相加会让「低血 + 连中」糊成纯红看不见路）：

1. 底噪 —— 剩余血量。从 **90** 开始渗（不是 70），曲线 `(1 - hp/90)^1.6 × 0.78`：
   让「我该包扎了」提前到还有得救的时候；
2. 事件 —— `hitFlash × 0.85`；
3. 濒死 —— 血量 < 40 时 `.hudDamage.low` 上 CSS 搏动动画（`hudPulse`，零 JS 成本）。

CSS 侧两处要点（`Style_Game.css`）：

- 过渡从 `0.25s` 压到 `0.09s` —— 红闪要跟得上枪响；
- 渐变停靠点从 `26% / 66% / 100%` 收到 `17% / 56% / 100%`。
  16:9 上「100%」是**最远那个角**（离中心 734 px），而屏幕上边缘只到 49%；
  停在 26/66 的话红几乎全堆在四个角上，视野正上下那两条带子基本没颜色 ——
  看起来就还是「没有红框」。

来弹指示是 `.hudHitDirs` 这个 SVG（`viewBox -100..100`，五个共用一份几何，
只改 `transform="rotate(deg)"`）。屏幕角 = `atan2(右分量, 前分量)`，
0° 正前、+90° 正右。

音频两条都**不给 position**：闷哼是自己嘴里发出来的，心跳在颅内，
都不该有房间混响与 HRTF 方位（`Script_Audio` 的 `heartbeat` 配方把 `wetGain`
写死成 0 就是这个意思）。一帧只哼一声 —— 一次爆炸的弹片能同帧结算好几下。

---

## 四、难度档接上线

`DIFFICULTY` 里三个一直没人读的字段，这一批接进了它们本来就该在的地方
（`docs/Data_EasyRed2Systems.md` 的「难度」那条写的就是这三处）：

| 字段 | 接在哪 | 体验 / 标准 / 写实 |
| --- | --- | --- |
| `aiAccuracy` | `Script_Ai.TryFire` 的 `acc` | 0.70 / 1.00 / 1.15 |
| `playerDamage` | `Script_Player.TakeHit` 的 `applied` | 0.80 / 1.00 / 1.25 |
| `suppressionScale` | `Script_Player.Suppress` | 0.60 / 1.00 / 1.30 |

`autoSurrender` 仍然恒 `false`，不给用户开 —— 这条不动。

---

## 五、复测（`Script_DamageTest.mjs`）

靶场不去等真实战场自己打起来：那件事取决于撒兵点、院墙和通视，重跑一次就是
另一个数，方差比要测的效果还大。测试把三个日本兵搬到玩家 25 m 上、目标焊死成玩家，
然后**走真的 `ai.TryFire`** —— 命中率、部位掷骰、伤害缩放、`TakeHit`、流血、红闪，
一条都不绕过去。唯一被替换掉的是「多久开一枪」（按武器表算的周期直接推时间），
因为那一段是计时器，不是规则。

靶场开在 **phase=1（`CH1_NanLu` 第一关 · 往南的路）**，且射手**只收持步枪的日军**（三支三八式）：

- phase=0 在任务流程重制后是不撒兵的过场序章，靶场一个射手都收不到。
  测试先「点名」（场上至少三个活着的持步枪日军）再开靶 —— 哪天又有章被改空，
  报出来的是判据不成立，不是 `Range.Run` 里一个看不懂的 TypeError。
- 「前 N 个活兵」拿到什么枪由撒兵顺序决定：混进一挺十一年式（周期 0.82 s 对
  三八式的 2.2 s）TTK 直接塌半，阈值就得跟着章走。焊死成三八式之后换章不动数。

四条采样纪律，踩过才写下来的：

- **时间轴按每一发推进**，不是按「一轮三发」推进。按轮推的话分辨率就是一整个
  射击周期（1.74 s），旧口径两轮死、新口径两轮死，读出来是同一个数，
  效果整个被量化掉（第一版就是这么读到「旧 5.2 s → 新 5.2 s」的）。
- **一组参数至少跑 15 次取中位数**。9 次时同一份代码两趟能读出 9.3 s 与 12.8 s，
  那个跨度比要测的效果本身还大，断言会随机变红。
- **「一枪毙」不能拿靶场里的单发最重去断言**。`worst` 是单发掉血的最大值，
  而只有一局的**第一发**打在满血上，后面的掉血天然被剩余血量截住 ——
  15 局 × 8% 爆头 ≈ 1.2 次，同样会随机变红（实跑遇到过一次）。
  改成拿旧倍率直接打一发爆头，确定性的。
- **手工救活玩家必须同时拆掉排队的换人**（`Range.Revive`，别直接 `player.Spawn()`）。
  `Range.Run` 是真的把玩家打死的，而装配层要到**下一帧**才发现
  （`OnPlayerDown` 排在所有伤害系统之后），那一帧会把 `state.deathTimer` 设成
  2.6 s、到点自动 `RespawnPlayer()`。`player.Spawn()` 只改玩家自己，倒计时
  照走 —— 于是换人会在**后面某个段落的测量窗口中间**落地，把血量刷回 100、
  暗角清零。第 6 节量 90 帧而残余倒计时约 1.5 s，前 89 帧 `.hudDamage.low`
  都在、最后一帧被擦掉，读出来就是一条稳定红的「濒死没进搏动」。

二十四条断言覆盖：点名（本章真撒了兵、且步枪兵够三个）、单发不致死、TTK 新旧对照、
首发必偏（40 发 0 伤害且照样压制）、红闪/暗角/来弹弧角度、闷哼与心跳真的被播、
姿态仍然分得出来、爆炸与白刃仍然重、手榴弹警告的亮/指向/脉冲/消失。

### 落地的数

**基线是在 `CH1_NanLu`（第一关 · 往南的路）上、拿三支三八式量的**，2026-08-29 起。
在这之前靶场挂在旧 `L0_Jiehe` 上；任务流程重制之后 `?phase=0` 是**零敌人的过场章**
（`CH0_Chuchuan`，`cutsceneOnly`、`ijaPool: 0`），在那儿挑不到日本兵，
射击周期算出来是 NaN、整份 TTK 报告变成一串 −1 **而且不报错**。
换章的逐项对照写在 `Script_DamageTest.mjs` 头注里；结论是被测对象没变 ——
CH1 与旧 L0 的 `ijaPressure` / `ijaSpawn` / `ijaSupport` / `ijaForce` 逐字相同。
**`COMBAT.player` 那十一个字段、`aiAccuracyBase`、`Data_Weapons` 的伤害一个数都没动。**

数字每次执行一模一样（按局播种的固定骰序 + 射手焊死成步枪兵）：

| | 旧口径 | 新口径（同一台靶场） |
| --- | --- | --- |
| 三人 25 m 站姿 TTK（15 次中位数） | 6.6 s | **16.1 s**（2.44×） |
| 单发最重 | 100（打死） | ≤ 47.5，在 `maxBulletDamage` 62 之下 |
| 靶场命中率 | ≈ 30% | ≈ 19% |
| 满血挨一发之后的暗角不透明度 | 0（血还在 70 以上） | ≈ 0.53（红闪）→ 0.07（底噪） |

姿态仍然是最大的一根杠杆：**立 16.9 s / 蹲 17.6 s / 卧 29.3 s**（11 次中位数）。
断言阈值：站姿 TTK 落在 8—24 s、新旧比 > 1.5、卧 > 立。

> **为什么比早年混编射手量的 9—13 s 整体长了三成**：那时候靶场收的是「前三个
> 活着的日本兵」，谁排在前面取决于撒兵，于是常混进一挺十一年式（周期 0.82 s
> 对三八式的 2.2 s），TTK 直接塌半。焊死成纯三八式之后一轮 2.2 s，射速与单发
> 伤害都低于混编 —— 这是口径换纯步枪的代价，不是回归，而且**换章不再动数**。
> 断言压的仍然是「卧 > 立」这条**关系**与那条 8—24 s 的宽带，不压绝对值。

> 注意靶场是**最坏情况**：无限弹药、不换弹、不被压制、通视永不中断、
> 三个人从头打到尾。真实关卡里的 TTK 只会比这个长。
