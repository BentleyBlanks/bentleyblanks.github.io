# 第一关重构验收

实现来源：[Notion 第一关新版](https://app.notion.com/p/3d360335331c81ea86f6f637ab92327c)。2026-09-07 的两轮补充覆盖正文：20 副担架、36 名轻伤员；南行喘息 1–2 分钟；真实转运 2–4 分钟；死亡短镜头 8–12 秒；外围命名采用村东来路、南向后送路、村后西侧沟道、临时便桥、城边接收院。

## 实现与验收清单

- [x] 同一 `?whitebox=p012` 和主菜单第一关入口直接使用新版流程
- [x] 行驶军列自由移动、两次近炮与混乱下车、炮后私语
- [x] 真正下挖的交通壕与射击踏步，渲染/角色/AI/碰撞共享高度
- [x] 掩护守军撤退，玩家接管机枪，按实际弹药报数
- [x] 玩家集束手榴弹炸断履带，炮塔仍作战，日军向村侧绕行
- [x] 顺子主动抢后送差事，1–2 分钟南行喘息
- [x] 20 副担架、36 名轻伤员分段汇入，担架员与医护替补
- [x] 村屋交火、大刀教学、开院门、按实际通过数报告
- [x] 2–4 分钟转运阻击：排队→分流→装车→车辆实际南下
- [x] 两轮空袭期间地面追兵继续作战，后送设施受损
- [x] 玩家接抬老周→2 秒松手扑沟→同伴抢救→玩家断后
- [x] 三处交替断后与真实担架通行，老周伤势逐步恶化
- [x] 接收院防御→亲手最后搬运→12 秒死亡短镜头→恢复战斗
- [x] 清除封锁后门射线、掩护医护、玩家走到城边联络巷完成
- [ ] 每段连续对话整段 Seed Audio；音频实际可读、单 cue 回放、字幕同步
- [x] 重试/暂停/恢复/再次开始无悬挂交互、重复演员或重复对白（新录音的听感仍待资产验收）
- [x] 新流程逻辑、地形通行、浏览器真实操作全流程与可见截图通过
- [x] import map 与字体版本核对（`202609070523`）
- [x] 114 项预推送检查完整通过，历史基线 0、失败 0；发布时另核对 Actions 提交与线上版本

## 范围决定

本次覆盖旧 P0–P2 场景入口，旧纯规则回归保留。动画仅使用现有基础姿态和白盒实例化表达，不制作精细蒙皮动作。环境不新增位图，使用纯色白盒几何；确需位图时走内置 imagegen。近战复用共享伤害与 QTE，提前射杀该名教学敌人按正文判定规则跳过，不补刷。

白盒限制：检查点只恢复玩家，伤员和队列保持实际状态，不会复活被杀死的伤员。如果在院门前误杀尚未通过的伤员，固定的 20 副担架通过条件无法满足，需要从暂停菜单重新开始关卡。

## 验证方式

`node Taierzhuang1938/Script_FirstLevelMissionTest.mjs` 检查任务事实门禁、共享地形、真实尺寸通路、20 副担架的排队装车和撤离、伤亡与替补、连续对白重试队列。`--audio` 是另一个严格资产门禁；音频未生成时不计为完成。

`node Taierzhuang1938/Script_FirstLevelMissionBrowserTest.mjs --campaign` 从行驶军列开始，用输入路由实际移动、射击、换弹、V 切大刀、F 交互和 H 投掷，完成全部阶段。它不写任务事实、不传送玩家、不修改敌人生命、不跳过搬运；一次明确的致死夹具专门验证死亡菜单重试。自动瞄准只调整玩家真实视角，射击继续经过正常射速、散布、弹道、弹药和命中判定。暂停、当前检查点按钮、渲染预算和三处节奏窗口也在这一入口验证。

本地截图和实测日志保存在忽略目录 `_shots/FirstLevelMission/`。阶段推进必须来自真实事件；纯计时不会完成开门、射击、救人、运输或撤离任务。

发布前继续执行项目 `prepush` 选测。声音严格验收为 `node Taierzhuang1938/Script_FirstLevelMissionTest.mjs --audio` 和 `node Taierzhuang1938/Script_FirstLevelMissionBrowserTest.mjs --audio --campaign`；后者实际点击开始按钮解锁音频，检查每段解码及 WebAudio 播放源，再以真实音频长度驱动全流程。最后核对线上版本。

最近一次完整通关包含老周开始登车、真实兵刃接触触发的 2.7 秒 QTE 窗口、两车实际南下、11 副剩余担架和 14 名医护从后门撤离。实测南行 75 秒、转运 226 秒、死亡短镜头 12 秒。原预推送选中的 114 项检查全部通过；菜单测试改用新版章节日期和地点后，从该项续跑剩余 70 项，未删减检查。完整逐项记录在本地 `Data_PrepushResult.json`。新音频资产尚未生成，当前交付为可玩的字幕白盒；配音及其严格音频验收仍是未完成项。

## 当前外部依赖

当前进程/用户/机器环境均未设置 `VOLCENGINE_API_KEY`。已登录 Edge 的 Seed Audio 页面可列出，但连接返回 timeout / Debugger unattached。已请求用户恢复连接或设置用户环境变量；关卡实现继续推进。
