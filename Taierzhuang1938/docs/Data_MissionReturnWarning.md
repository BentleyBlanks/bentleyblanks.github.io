# 第一关偏离任务返回警示

用户 2026-09-14 要求参考 COD 等线性战役：远离小队、行动路线或任务人物时，以全屏提示要求返回，提前解释可玩区域的边界。

当前接入 `?whitebox=p012` 的第一关完整任务。`Data_FirstLevelMissionReturn.mjs` 按内部步骤配置有效路线，包含上一任务到新目标的合理过渡；领取集束弹、绕院、转运和撤离使用实际路线。`CurrentGuide()` 提供当下交互目标，包括接替担架时老周的当前位置。抬担架与救援阶段独立检查和老周的距离，防止玩家仍在路线内却远离伤员。行进阶段额外检查与罗班长的距离，抵达当前目标时允许等候队伍。其他章节还未接入。

`Script_MissionReturn.mjs` 是纯规则，不改变任务事实、玩家位置、伤害或碰撞。路线外偏离、队伍脱节、接近场景 bounds 三种原因共用进入延时、阶段宽限和退出迟滞。参数的唯一来源是 `Data_Tuning_FirstLevel.mjs` 的 `MISSION_RETURN`，属于本游戏设计值，并非 COD 原版参数。列车行驶、扑沟、牺牲演出、死亡与完成时禁用；暂停冻结计时，切关清空旧状态。

警示覆盖全视口，采用半透明暗角、标题、原因及相对朝向箭头。严重偏离显示“立即返回”。箭头在偏离路线时先指回最近的路线点，其他情况指向队伍或当前目标。接近外缘时仍保留世界防越界约束，提前提示取代无解释的首次撞墙体验。警示不捕获输入，不抢镜头，不弹确认按钮，不倒计时处死或传送；玩家回到范围内自动消退。

验证：`Script_MissionReturnTest.mjs` 检查阶段路线、迟滞、宽限、远目标、队伍距离、边界、方向和清除；`Script_FirstLevelMissionReturnBrowserTest.mjs` 在真实第一关检验提示、升级、键盘移动、返回、死亡和新任务清理，截图固定 1280×720，保存在忽略目录 `_shots/MissionReturn`。新版完整流程沿用 `Script_FirstLevelMissionTest.mjs` 和 `Script_FirstLevelMissionBrowserTest.mjs --campaign`。

## 2026-09-14 本次验证

- 返回规则、文本与字体、模块图、测试登记检查通过；真实浏览器的提示延时、严重偏离、键盘移动、返回消退、死亡与切关清除通过。最新主线整合后再次通过返回定向检查。
- HUD 提示、死亡菜单、MotionVectorContract 和发布包启动检查通过。第一关纯规则检查（含配音严格检查）通过。
- 从公开阶段 8 开始的阶段续接一直到结尾通过（原脚本 `--campaign --stage-jumps --stage-from=8`，并非从开场自然通关）。开场/前沿与后段累计 69 个正常流程快照中，返回警示均为空。
- **整关自动通关未通过，不能报告全绿。** 本次在 Tank 阶段缺少 `tankImmobilized`；干净基点 `804825af3` 的完整对照更早停在 Support，缺少 `zhouGunWounded`，因此没有证实两次为同一失败。随后在两棵树上从阶段 5 单独执行原脚本的补给、侧沟移动、投掷与 Orders 门，两边均通过；该对照使用无敌调试以排除战斗死亡，是步骤隔离证据，不替代自然通关。
- 截图与对照探针、日志均留本地；本次不修改战车命中、通关门、敌我 AI 或物理碰撞。
