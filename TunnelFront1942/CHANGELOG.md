# TunnelFront1942 轮次记录

每轮 = 设计 → 实现 → 完整游玩 → 独立审查 → 修改或删除 → 再次验证 → 提交 + 部署。
独立审查的五项标准与 verdict 逐轮记录于此。

## R3（2026-07-31）五幕战役重构 —— 按电影《地道战》的进化线重写关卡

用户判词：「没有剧情、没有推进，两张关卡之间没有因果。」本轮把两张抽象关卡换成**五幕战役**：
地道从「各家的藏身地窖」一步步变成一件武器，每一幕都由**上一幕的失败**推动。
**关卡不再是难度递增，是认知递增**——`level.unlocks` 是它在代码里的落点。

- **五幕（A1~A5）替换 L1/L2**（L1/L2 保留为别名 `L1→A1`、`L2→A5`，旧链接不断）：
  A1《单口洞》8T → A2《户户相通》10T → A3《能打的地道》10T → A4《毒烟与水》12T → A5《反扫荡》14T。
  A1~A3 共用高家庄同一张图——同一个村子，一幕比一幕会打，玩家能亲身比较
  「同样被搜出，第一幕跑不掉，第二幕跑得掉，第三幕还能还手」。
  每幕带档案式开场简报 + 三行「这一幕你要学会什么」+ 本幕新解锁清单 + 终局复盘三段
  （付出了什么代价 / 学到了什么 / 解锁了什么）。
- **`level.unlocks` 解锁过滤真的生效**：`LegalActions` 与 `PerformAction` 双向按幕过滤，
  后一幕的动作集必须是前一幕的超集。**第一幕挖不了连通段、开不了口、修不了设施**——
  它只有藏粮、转移群众、掩土、破路四件事，而这四件事注定不够。
- **第一幕注定要付代价（设计，不是失衡）**：明存粮 8 + 平静期 1 > 粮窖上限 4；
  群众 6 批（7 个铺位）> 两个人窖合计 4 个铺位；搜庄队 `levyAlways` 搜粮抓人一起来。
  胜利线放宽到「保住半数群众」，但**代价簿结构性不为零**（冒烟里既有结构断言也有多 bot 多 seed 实测）。
  终局复盘点破：「洞是死的，人是活的。洞不通，人就没处去。」
- **带领百姓躲避（用户明确要求的第二条主线，第二幕起贯穿到底）**：
  `state.wounded` 与 `village.pop` / `cell.civs` 全部删除，改为 `state.civs` 群众批注册表，
  分老弱（1 格/回合，1 铺）/ 青壮（2 格/回合，1 铺）/ 伤员（1 格/回合，2 铺）。
  **地道里的群众必须有单位同格带路才能移动**（`GuideCivs`，主动作；一次带 3/2/1 批，
  走的格数按最慢一批算）；无人带路每回合恐慌 +1，无风 +1、烟 +2、水 +2，同格有人则 -1。
  **恐慌 ≥3 → 自行冲出地面**，与「憋闷被迫出洞」合并为同一套压力，地面有敌当场被抓。
  终局按**群众保全率**评定。带路占主动作 →「谁去带路、谁去挖、谁去打」成为每回合的真取舍。
- **逐幕新机制**：A2 连通挖掘/多口/伪装口（灶台+1、炕洞+1、水井+2 但群众上不去、牲口槽带路 +1 批）；
  A3 射击孔 + 翻板 + **打一枪换一个地方**（同孔 1 回合内再开火只伤 1，且该格获「敌已警戒」）；
  A4 敌烟/水/刨口全开，反制全部来自结构（翻板挡烟挡水并拦敌一回合、拐弯让烟多花一回合、
  通气孔、**高低双层：水只往同高或更低处漫**、隔断门分区）；A5 三村相连 + 跨村地道网 + 全员转移 + 多孔协同。
- **敌反地道四选一 → 六选一**：新增**刨口**（不需工兵，A2 起）与**灌水**（需工兵与水车，A4 起）。
  盯上一个口的门槛改为 `4 + 伪装加成 × 2`——伪装口因此不只是更难搜出，也更晚被盯上。
- **HUD 数据契约**（渲染不归本轮，数据齐备）：`DeriveView` 新增 `act / newUnlocks / unlockedActions /
  unlockedFacilities / unlockedDisguises / civGuidance / objectiveProgress / civs / civSummary /
  network / debrief`，`result` 增 `civSafe/civTotal/civRatio/civForcedOut/act/actName/nextId`，
  `hexBadges` 增 `elev/water/trapReady/fightpost/entrance.disguise/civs/civPanic/civEscorted`，
  护栏新增 `kind:"panic"`。字段清单写进 `AGENTS.md §三.1`。
- **冒烟 48/48 全绿**（10.5s），其中 R3 承重墙回归 12 条，逐条验证**先红后绿**
  （去掉解锁过滤 / 恐慌归零 / 取消射击孔锁定 / 让水爬坡 / 取消拐弯延迟，五处破坏各自只打红对应那一条）。
  R2 的全部承重墙原样保留并通过。
- **五幕逐回合手动通关实测**（`Script_PlayCli.mjs`，seed 3）：
  A1 胜/乙（群众 5/6，代价簿：被抓 1 批 + 被夺 5 担）、A2 胜/乙（6/6，被夺 4）、
  A3 胜/乙（6/6，被夺 6，两个射击孔各打一枪后被迫用热孔只伤 1）、
  A4 胜/**甲**（8/8，通气孔 2 + 高处粮窖 + 隔断门切断东洼，零逼出）、
  A5 胜/乙（10/10，三村贯通，T12 判定敌扑空收队）。
- **策略排序**（4 bot × 5 幕 × 8 seed）：会玩 A1~A5 各 8/8 通关；缩头只在第一幕能过且永远只有丙；
  莽撞第二幕起 0/8；乱打第二幕起 0/8。
- 契约变更同步至 `AGENTS.md` §〇/§2.0/§2.1/§2.2/§2.3/§2.6/§2.8/§2.9/§2.10/§2.11/§2.12/§三/§三.1/§四/§五/§7.1。

## R2（2026-07-31）玩法内核修复 —— 独立盲审五项全 FAIL 的逐条返工

盲审（只读 README + CLI，L1 三局 + L2 两局逐回合游玩 + 32 局 bot 对照 + 2 组定向实验）判定五项标准全 FAIL。
本轮不加新单位、不加新资源、不加新子系统，只改规则与数值。修前/修后同口径实测：

- **P0-1 暴露豆全程结算**：原本只在扫荡开始那一瞬结算（定向实验：扫荡期挖 5 段开 2 口，所有口仍是 0 豆）。
  现在每挖通 1 段 / 修成 1 设施 → 同气区各口 +1，每开新口 → 该口 +2；掩土 -2 但**每人每局 3 次**。
  实测「只挖不掩土」流的口最高暴露从 0 → 30~46 豆。
- **P0-2 烟攻/爆破真的会发生**：作业门槛从「已被搜出（9 豆）」降到「暴露豆 ≥4」；烟攻加条件
  「该气区通风口 < 地道格数/3」；封堵改为每纵队每役至多 1 次；工兵队会主动奔向全图暴露最高的口，
  走到即在同回合下电报。L1 补入工兵组（烟具 1）。**25 局 0 次 → 10 seed 实测 L1 烟 4/爆 3/封堵 11，
  L2 烟 13/爆 3/攻入 10/封堵 19。**
- **P0-3 憋闷改成真的「被迫出洞」**：原实现是原地掉血直到窒息阵亡（与 README 相反）。现在憋满 3 回合
  **一定上地面**：先走分区最近的未封口，没有口就在本格正上方刨土钻出（留痕迹），连正上方不可通行才掉血；
  地面有敌 → 群众当场被抓进代价簿。这是地上-地下唯一的强耦合点。
- **P0-4 HideGrain 要求物理连通**：本村格**正下方**必须有地道格且连着还装得下的储粮洞、气区还有未封的口。
  盲审「头顶一格地道都没有的村里成功储粮」的复现用例现在被拒绝。
- **P0-5 拆同格叠伏击 + 消灭重复劳动**：同格至多 1 人设伏；伏击改为**持续状态**（不必每回合重按）；
  打完强制转「暴露」并当场吃一次还击；同格连设两回合 → 伤害 3→1 且该格获「敌已警戒」2 回合（敌寻路 +4 绕行）；
  民兵可在任何可挖地形挖散兵坑设伏（+1 痕迹）——民兵在开阔地不再瘫痪。
- **P0-6 修好 L1**：胜利线「总粮 ≥8」→「**洞存粮 ≥10**」，明存粮在敌到村 2 格内每回合被搜走 2 担；
  池 10→14 且**取消每回合白送的 -1**，改为**扑空衰减**（摸到村边却一无所获的纵队每支 -1，每回合至多 -2）；
  地图补南土路、浅滩，石桥改为可破；seed 给到真变量（轴线 9 × 配比 3 × 到达 T5±2 × 斥候 6 × 报复队 2）。
  **12 个 seed 的终局从「逐字节相同」→ 9 种不同终局；逼退时点从 T7-T8 → T9-T10（10/12 seed）。**
- **P1**：同格敌人可攻击；石桥可破且破后敌走浅滩/南土路绕行；弹药收支改为严格为负（日军班缴获 2→1，
  伪军/工兵 →0，反击致死不缴获）；敌在无粮可征的村里改为抓丁（civCaptured 活了）；
  L2 三枚勋记各加一条「全须全尾」判据（丢 8 担粮 + 死一个班的一局不再与全须全尾同评）。
- **盲审两大反例复验**：零地道流 胜/甲/●●● → **负/丁/○○○**；三单位堆一格复读伏击 胜/甲/●●● → **负/丁/○○○**。
- **冒烟 39/39 全绿**（6s），其中 R2 新增承重墙回归 11 条（先红后绿，逐条可单独变红）。
- **铁律自查**：`Data_Levels.mjs` +656 行 > 全部 `Script_*.mjs` 增长合计 +652 行——加的是内容与选择空间。
- 契约变更同步至 `AGENTS.md` §2.3/§2.5/§2.6/§2.7/§2.8/§2.9/§2.10/§2.12/§三/§四/§7.1。

## R1（2026-07-30）v0.1 可玩白盒落地

- **引擎**（纯逻辑约 3900 行）：`Script_State/Actions/Visibility/EnemyAi/Turn/Bots` +
  `Data_Rules/Data_Levels`。全确定性、零 `Math.random`、CloneState 推进、事件带 `visible` 标记。
- **表现层**（约 2900 行）：`Script_Renderer/Ui/Main` + `Data_Theme/Data_Keymap` + `index.html/Style_Game.css`。
  双主态视图（Q 切层、Space 窥视）、格上徽记预算、两段式结束回合护栏、敌阶段按可见性播报、三勋记结算。
- **审查接口**：`Script_PlayCli.mjs`（new/show/legal/act/end/run/fixture）+ 双层 ASCII 地图。
- **冒烟 26/26 全绿**（2.6s）：契约/确定性/乱打模糊/策略排序红线/双时钟/佯动木偶化/反地道四出口/
  憋闷链/预算比率/账本红线/kills 封顶/页面装配。
- **Balance**（4 bot × 2 关 × 8 seeds）：L1 Skilled 100%(8甲) / Turtle 0% / Rambo 0%；
  L2 Skilled 100% / Turtle 0% / Rambo 63% / Random 25%。
- 浏览器实测：真引擎 `?level=L1&seed=3` 零控制台错误，回合推进正常，双层渲染与 HUD 可辨认。
- 引擎侧关键裁决：纵队整体制（同格同进退）；邻接接火只打得动的目标（防止征粮队被隐蔽民兵空转）；
  受创斥候回撤报信；L2「占村」需有可剿之物（藏净粮+塌已知口 = 让敌扑空，核心策略闭环）；
  `DeriveView` 改为纯函数（原写 `enemy.lastSeen` 会让调用视图与否的哈希分叉）。
- 已知问题（下轮）：L1 对 Random 过宽（乱打也能拿丙）；Rambo 在 L2 仍有 63% 胜率。

## R-设计（2026-07-30）七领域设计委员会 → 规则 v2 裁决

- 七个领域 Sub-Agent（核心玩法/双层地图/单位战斗/敌军AI/关卡任务/操作反馈/数值平衡）并行评审。
  （工作流权限层故障导致其文件读取与结构化输出被剥参，全部结论自转写抢救合并；各 Agent 按简报独立推导。）
- 关键裁决（详见 AGENTS.md §二 v2）：
  - 保留文明式逐单位 MP+主动作操作；否决抽象共享行动池。
  - **敌军不以单位进入地道**：攻入改为提前电报的一回合抽象结算（有驻守=敌损1班中止；无=毁口夺物）。
    删陷阱/休息室/地道内逐格战斗（-500 行）。
  - v1 反地道 = 搜索暴露 + 烟攻(逼出而非杀伤) + 爆破(可打断) + 封堵；灌水推迟 v2（河仍为地形约束）。
  - 入口暴露 = 隐蔽等级×3 暴露豆，确定性、全明牌、只增不减；掩土只作用于挖掘痕迹。
  - **挖掘痕迹结转**：平静期每次挖掘 +1 痕迹，扫荡开始转为入口暴露豆初值——反无脑挖网的账。
  - 佯动防木偶化：目击置信两级，置信 1 绝不拉动纵队；机动队专责响应置信 2。
  - 敌撤退 = 行动力池（歼敌/破路/反地道自耗扣池，单回合上限 5，绝不产生玩家资源）。
  - 评级 = 三勋记制（只奖保全与保网），歼敌不进 UI 计分；数值分仅供冒烟排序断言。
  - 民兵不能野战攻击（仅伏击/枪眼）；弹药只靠缴获且净收支为负；无征募，损失永久。
  - 憋闷 3 回合被迫出洞且先于敌行动结算——反缩头的确定性机制。
  - L1《护粮》10 回合单村教学（含条件触发报复队）；L2《合围》16 回合三村三阶段脚本；seed 白名单只变敌不变图。

## R0（2026-07-30）奠基

- 建项目：vendor three r160（复用 PrairieFire1937）、`Script_Hex.mjs` 地基库移植。
- `AGENTS.md`：题材红线、玩法规则 v1 设计种子（双层六角/地道系统/敌军反制/双时钟紧张引擎/
  防退化策略表）、状态契约、动作协议、模块所有权、测试门槛（含策略排序红线）。
- 下一步：七领域设计委员会并行评审 → 规则 v2 → 引擎与表现层并行实现。
