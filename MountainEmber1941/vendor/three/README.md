# Three.js runtime subset

This directory contains the minimal runtime subset used by `MountainEmber1941`, vendored from `three@0.160.0` under the included MIT license.

The module files use the `.mjs` suffix so static servers return a JavaScript MIME type. This keeps the game available without a runtime CDN.
