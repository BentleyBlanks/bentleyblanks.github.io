// 运行时调试开关：可由调试面板即时翻动的临时开关（不进存档、不进 TUNING 平衡表）。
// 这里只放「要不要触发某个机制」这类临时旁路，真平衡数值仍在 tuning.ts。
export const DEBUG_FLAGS = {
  // §特殊越界请求：当前版本默认【关闭】，不再随机弹出「搬空钱包」之类的特殊请求卡。
  // 调试面板里有一个开关可临时打开，用于测试该机制。
  specialRequests: false
};
